project:
  type: manuscript

manuscript:
  article: index.qmd

format:
  html:
    comments:
      hypothesis: true

  # docx/jats/pdf disabled: the figures are interactive Observable/D3
  # visualizations (ojs cells), which only execute in the html format.
  # docx: default
  # jats: default
  # pdf: default

execute:
  freeze: auto



